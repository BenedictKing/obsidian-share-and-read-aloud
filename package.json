{
  "name": "obsidian-share-and-read-aloud",
  "version": "0.2.2",
  "description": "Clean Markdown into readable text. Share, copy, or read aloud with TTS.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.15",
    "builtin-modules": "3.3.0",
    "esbuild": "^0.25.8",
    "obsidian": "^1.8.7",
    "tslib": "^2.8.1",
    "typescript": "^5.8.3"
  }
}
